# 内容安全策略（CSP）

## 学习目标

- 知道 CSP 通过响应头或 `meta` 声明「允许加载/执行哪些资源」。
- 理解 CSP 作为 **深度防御** 与 XSS 防御手段（转义、模板、框架默认行为）之间的关系。

## 正文

CSP 可以显著降低 XSS 的「可执行面」，但配置不当也会导致功能损坏。下方实验区在 **iframe 隔离文档** 中注入 CSP，并检测**内联脚本**是否执行；结果因浏览器与策略细节可能略有差异，以规范与实测为准。

<CspLab />
